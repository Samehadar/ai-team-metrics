# No environment variables are required for basic usage.
# The dashboard works entirely client-side with uploaded CSV/JSON files.
#
# Optional external activity provider (per-developer GitLab/VCS heatmap):
# copy .env.local.example to .env.local and fill credentials.
